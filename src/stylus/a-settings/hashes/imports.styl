/* ==========================================================================
   SETTINGS - HASHES - IMPORTS
   ========================================================================== */

imports = {

  base: {
    blockquotes				    : true,
    embedded 					    : false,
    headings 					    : true,
    inputs 						    : false,
    labels 						    : false,
    links  						    : true,
    lists 			 				  : true,
    paragraphs 				    : true,
    tables 						    : false,
    buttons 					    : true,
  },

  objects: {
    wrapper               : true,
    media                 : false
  },

  layout: {
    grid-silent 			    : true,
    grid-classes 		      : false,
  },

  navigation: {
    nav-site 				      : false,
  }

  utilities: {
    colours 		          : false,
    visibility 	          : true,
    widths     	          : false
  }

}
