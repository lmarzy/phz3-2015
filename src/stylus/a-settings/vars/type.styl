/* ==========================================================================
   SETTINGS - VARIABLES - TYPOGRAPHY
   ========================================================================== */

font-size 					   = 16px;
font-family 				   = 'Raleway', sans-serif;
line-height 				   = 24px;
font-url 				       = 'https://fonts.googleapis.com/css?family=Raleway:400,700';

heading-font-family 	 = 'Raleway', sans-serif
heading-font-size 		 = inherit;
heading-font-weight 	 = 700;
