/* ==========================================================================
   COMPONENTS - HEADER
   ========================================================================== */

.c-site-header {
  color: lighten(colours.brand.b,30);
  background: colours.brand.b;
  text-align: center;

    +bp('20', max) {
      display: flex;
      height: 100vh;
      align-items: center;
      justify-content: center;
      padding: 0 rem(12px);
    }
}
